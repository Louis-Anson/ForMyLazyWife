# AiJiaS - 家庭资产管理服务
version: '3.8'

services:
  homebox:
    image: sysadminsmedia/homebox:latest      # 官方 Docker Hub 镜像
    container_name: homebox
    restart: unless-stopped
    volumes:
      - /share/Container/homebox/data:/data
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=Asia/Shanghai
      # 可选：设置日志级别
      - HBOX_LOG_LEVEL=info
      # 可选：禁止用户注册（管理员手动创建账号）
      - HBOX_ALLOW_REGISTRATION=false
    networks:
      - traefik-public
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.homebox.rule=Host(`homebox.aiJiaS.local`)"
      - "traefik.http.routers.homebox.entrypoints=websecure"
      - "traefik.http.routers.homebox.tls.certresolver=letsencrypt"
      - "traefik.http.services.homebox.loadbalancer.server.port=7745"

networks:
  traefik-public:
    external: true
